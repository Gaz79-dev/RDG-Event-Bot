DISCORD_TOKEN=YOUR_ACTUAL_DISCORD_BOT_TOKEN_HERE

# Optional: Set your host user's UID and GID for better file permission handling with volumes.
# On Linux/macOS, you can find these by running 'id -u' and 'id -g' in your terminal.
# If you don't set these, the Dockerfile will default to 1000.
# UID=YOUR_HOST_USER_ID
# GID=YOUR_HOST_GROUP_ID
